# Experiment 4 Report Template

## 4.1 Hybrid topology + DHCP + manual IP configuration
1. List components used (routers, switches, CAT6 cables, PCs/laptops).
2. Draw rough logical topology (star + tree hybrid).
3. Configure router interface IP and DHCP pool.
4. Configure selected nodes with dynamic IP (DHCP).
5. Configure remaining nodes with manual static IP.
6. Verify connectivity using ping between multiple nodes.
7. Attach step-by-step lab photos with captions.

## 4.2 Shared folder and file access
1. Create shared folder on admin machine.
2. Set network permissions for all users.
3. Create file named with your registration number.
4. Copy that file into shared folder.
5. Access same folder from other client nodes.
6. Attach step-by-step lab photos.

## Admin storage allocation note
- Allocate a fixed disk space (example: 1 GB) from admin machine.
- Assign user quotas (example: 10 MB each).
- Verify users can read/write from any node in same network.

(Replace this template text with your real lab execution details and photos.)
